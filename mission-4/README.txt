Missão 4 — Didact (componentes de função + useState)

Como rodar:
  cd mission-4
  python3 -m http.server 8080

No navegador: http://localhost:8080

Arquivos:
  didact.js   — M3 + updateFunctionComponent + useState; Didact = { createElement, render, useState }
  index.html  — carrega didact.js + teste Greeting (createElement, sem JSX)
